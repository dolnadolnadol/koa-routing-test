meta {
  name: add-ticket
  type: http
  seq: 2
}

post {
  url: {{HOSTNAME}}/tickets
  body: json
  auth: none
}

body:json {
  {
    "title": "test typrdifef add",
    "description": "description test typrdi add"
  }
}
